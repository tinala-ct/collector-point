# 🎡 Classroom Lucky Wheel & Score Collector
## 📋 เอกสารข้อกำหนดระบบและการส่งต่องาน (Project Specification & Handover Document)

> **วัตถุประสงค์ของเอกสารนี้:** รวบรวมความต้องการทั้งหมดของโปรเจกต์ โครงสร้างสถาปัตยกรรม โค้ด และสถานะการพัฒนา เพื่อให้ผู้พัฒนาหรือ AI ตัวอื่นสามารถเข้ามาทำความเข้าใจและสานต่องานได้ทันทีโดยไม่ต้องเริ่มใหม่จากศูนย์

---

## 📌 1. ข้อมูลภาพรวมของโปรเจกต์ (Project Overview)

- **ชื่อโปรเจกต์:** Classroom Lucky Wheel & Score Collector (วงล้อสุ่มชื่อ & ระบบสะสมคะแนนในชั้นเรียน)
- **กลุ่มเป้าหมายผู้ใช้:** คุณครู, ผู้จัดกิจกรรมการเรียนการสอนในชั้นเรียน, วิทยากร
- **URL เว็บไซต์จริง (GitHub Pages):** `https://tinala-ct.github.io/collector-point/`
- **GitHub Repository:** `tinala-ct/collector-point` (Branch: `main`)
- **เทคโนโลยีที่ใช้ (Tech Stack):**
  - **HTML5 & CSS3:** Semantic HTML, CSS Custom Properties (Variables), Responsive Grid/Flexbox, Claymorphism 3D styling.
  - **ES6 JavaScript (Modular):** สถาปัตยกรรมแบบแยกโมดูล (ES Modules) ชัดเจน ไม่ใช้ Framework หนักๆ ทำให้โหลดเร็วมาก
  - **HTML5 Canvas 2D:** วงล้อหมุนที่ลื่นไหล พร้อมระบบคำนวณมุมองศาและการชะลอความเร็ว (Cubic Ease-Out)
  - **Web Audio API:** สังเคราะห์เสียงเอฟเฟกต์ด้วยโค้ด 100% (เสียงติ๊กวงล้อ, เสียงตอบถูก, เสียงตอบผิด, เสียงดนตรี Fanfare, เสียงหักคะแนน) โดยไม่ต้องโหลดไฟล์ `.mp3` ภายนอก
  - **LocalStorage:** บันทึกรายชื่อนักเรียนหลายห้องเรียน คะแนนสะสม และการตั้งค่าไว้ในเครื่องแบบ Offline 100%

---

## 🎯 2. สเปกฟังก์ชันหลักทั้ง 3 โหมด (Core 3 Game Modes)

ระบบถูกออกแบบให้มี **ปุ่มสลับ 3 โหมดกิจกรรม (Mode Switcher)** อยู่ที่แถบด้านบนของหน้าเว็บ:

```
[ 🎡 สุ่มวงล้อ ]   [ 🚶 เข้าแถวตอบ ]   [ 📊 กราฟตัดแต้ม ]
```

---

### โหมดที่ 1: 🎡 วงล้อสุ่มชื่อ (Random Wheel Mode)
1. **วงล้อหมุน Canvas 2D:**
   - หมุนสุ่มชื่อนักเรียนจากห้องเรียนที่เลือก
   - ตำแหน่งลูกศรชี้อยู่ที่ **3 นาฬิกา (0 เรเดียน ทางขวา)**
   - เมื่อหมุนหยุด จะอ่านชื่อชิ้นวงล้อที่อยู่ใต้ลูกศรอย่างแม่นยำ 100%
2. **ระบบสุ่มอัจฉริยะ (Smart Balanced Randomness):**
   - มี 3 อัลกอริทึมให้เลือกใน Settings:
     - 🌟 **กระจายตัวสมดุล (Smart Balanced - ค่าเริ่มต้น):** คำนวณน้ำหนักตัวเลขสุ่ม โดยบูสต์นักเรียนที่ยังไม่เคยได้ตอบ หรือตอบน้อยกว่าเพื่อน พร้อมลดโอกาสคนที่เพิ่งตอบไปในรอบที่แล้ว เพื่อไม่ให้สุ่มโดนคนเดิมติดกัน
     - 🎴 **หมุนเวียนครบทุกคน (Shuffle Bag):** สุ่มรายชื่อในห้องแบบจับฉลากไม่ซ้ำจนครบทุกคน 1 รอบ แล้วเริ่มรอบใหม่
     - 🎲 **สุ่มอิสระทั่วไป (Pure Random):** สุ่มอิสระแท้จริงตามค่าความน่าจะเป็น
3. **ป๊อปอัปให้คะแนนเมื่อสุ่มได้นักเรียน:**
   - **`✅ ตอบถูก (+1 คะแนน)`**: ได้รับคะแนนสะสม พร้อมเสียง Chime และพลุกระดาษ Confetti
   - **`❌ ตอบผิด (0 คะแนน)`**: ไม่ได้คะแนน พร้อมเสียง Buzzer นุ่มนวล
   - **`⭐ โบนัส (+2 คะแนน)`**: ได้รับคะแนนพิเศษ
   - **`⏭️ ข้าม`**: ข้ามไปสุ่มคนใหม่โดยไม่นับรอบ

---

### โหมดที่ 2: 🚶 เข้าแถวเรียงตอบ (Sequential Character Queue Mode)
1. **ตัวละครดินปั้น 3D White Clay Figurine:**
   - กราฟิกตัวละครหุ่นดินปั้นสีขาวมินิมอล 6 ท่าทาง (ชูแขนเบ่งกล้าม `flex`, ยืนตรง `stand`, กางแขนกระโดด `cheer`, ประสานมือบนหัว `stretch`, นั่งคุกเข่า `kneel`, โน้มตัว `lean`)
   - ทุกตัวละครมี **ป้ายชื่อนักเรียน** และ **เหรียญคะแนนสะสม** ติดตัว
2. **กลไกการตอบและวนแถว:**
   - คนหน้าสุดของแถวจะยืนอยู่บนแท่น **Spotlight หน้าห้อง** ขนาดใหญ่
   - เมื่อกด **ตอบถูก (+1)**: ตัวละครจะกระโดดดีใจ มีพลุกระดาษ ได้คะแนนสะสม แล้วเดินไปต่อท้ายแถว
   - เมื่อกด **ตอบผิด (0)**: ตัวละครแสดงท่าเสียดาย แล้วเดินไปต่อท้ายแถวเพื่อรอตอบรอบถัดไป
   - คนถัดไปในแถวจะก้าวขึ้นมาหน้าห้องอย่างต่อเนื่องโดยอัตโนมัติ

---

### โหมดที่ 3: 📊 กราฟแท่งตัดคะแนนพฤติกรรม (Behavior Point Deduction Mode)
1. **กฎและเงื่อนไขของโหมดนี้:**
   - นักเรียนทุกคนเริ่มต้นคาบเรียนด้วย **คะแนนเต็ม 100 คะแนน** (สามารถปรับเปลี่ยนใน Settings ได้ เช่น 50, 100, 200)
   - แสดงผลเป็น **กราฟแท่งพลังชีวิต (Health Bar / Behavior Bar)** สำหรับนักเรียนแต่ละคน
   - สีของแท่งคะแนนจะเปลี่ยนตามระดับคะแนนที่เหลืออยู่:
     - 🟢 **80 - 100 คะแนน**: ยอดเยี่ยม (Full HP / Excellent)
     - 🟡 **50 - 79 คะแนน**: ปานกลาง / เริ่มต้องตักเตือน (Warning)
     - 🟠 **20 - 49 คะแนน**: ต้องปรับปรุง (Critical)
     - 🔴 **1 - 19 คะแนน**: วิกฤต (Danger Zone)
     - 💀 **0 คะแนน**: หมดพลัง (Empty)
2. **ปุ่มกดลดคะแนนรายชื่อ `[-5 แต้ม 💥]`:**
   - แต่ละรายชื่อจะมีปุ่มลดคะแนนทีละ 5 คะแนน **(ไม่มีปุ่มเพิ่มคะแนน)**
   - เมื่อกดลดคะแนน:
     - หักคะแนนนักเรียนคนนั้นทีละ 5 แต้ม (หยุดที่ 0 ไม่ติดลบ)
     - มีเสียงเตือนน่ารักๆ (Soft Boop SFX)
     - มีแอนิเมชัน **ตัวเลข `-5` สีแดงลอยขึ้นมา** (Floating text)
     - การ์ดของนักเรียนจะสั่นเตือน (Shake animation)
     - แถบพลังชีวิตจะลดความยาวลงทันที
3. **เครื่องมือควบคุมบนเวที (Toolbar):**
   - แสดงสถิติ: คะแนนเริ่มต้น, คะแนนที่หักต่อครั้ง, จำนวนครั้งที่ถูกหักรวมทั้งห้อง
   - ช่องค้นหาชื่อนักเรียน (Search Filter)
   - ตัวเลือกจัดเรียง: เรียงตามเลขที่, เรียงคะแนนมาก ➔ น้อย, เรียงคะแนนน้อย ➔ มาก
   - ปุ่ม `🔄 คืนค่าเต็ม 100`: รีเซ็ตคะแนนทุกคนกลับเป็น 100
4. **การมอบรางวัลในโหมดตัดคะแนน:**
   - เมื่อกดปุ่ม **"🏆 สรุปผลรางวัล"**:
     - แท่นรับรางวัล **Podium (1st 🥇, 2nd 🥈, 3rd 🥉)** จะมอบให้แก่ **นักเรียนที่รักษาคะแนนคงเหลือไว้ได้มากที่สุด** (เด็กดีมีวินัยประจำคาบ)
     - เหรียญฉายาพิเศษ: 🌟 *เทพบุตร/นางฟ้าประจำคาบ (100 HP Club)*, 🛡️ *โล่เหล็กวินัยดีเด่น (Zero Deductions)*, 🎖️ *นักสู้ผู้รักษาวินัย*

---

## 🏆 3. ระบบสรุปผลและส่งออกข้อมูล (Podium & Export)

1. **แท่นรับรางวัล Podium (อันดับ 1, 2, 3):**
   - มีเสียงดนตรีเฉลิมฉลอง Fanfare และพลุกระดาษ Confetti
2. **สถิติวิเคราะห์ชั้นเรียน:**
   - คะแนนสูงสุด (Max), คะแนนต่ำสุด (Min), คะแนนเฉลี่ยห้อง (Average)
3. **ส่งออกไฟล์ Excel/CSV:**
   - ไฟล์ `.csv` ที่มี **UTF-8 BOM** (เปิดใน Microsoft Excel ภาษาไทยไม่เพี้ยน ไม่เป็นภาษาต่างดาว)
   - มีปุ่มพิมพ์รายงาน (Print Friendly)

---

## 📂 4. โครงสร้างไฟล์ทั้งหมดในโปรเจกต์ (File Map)

```
collector-point/
├── index.html              # หน้าเว็บหลัก, Modal ต่างๆ, Header switcher
├── README.md               # คู่มือการใช้งานสำหรับผู้ใช้
├── PROJECT_SPEC.md         # [ไฟล์นี้] สเปกข้อกำหนดทางเทคนิคและการส่งต่องาน
├── css/
│   ├── main.css            # ธีมสี (Light/Dark), ฟอนต์, ปุ่ม, เลย์เอาต์หลัก
│   ├── wheel.css           # สไตล์วงล้อ Canvas, เข็มชี้, เอฟเฟกต์แสงเงา
│   ├── queue.css           # สไตล์ตัวละครดินปั้น 3D, แท่น Spotlight, แถวรอคิว
│   ├── deduct.css          # สไตล์กราฟแท่งตัดคะแนน, หลอดพลังชีวิต, แอนิเมชัน -5
│   └── scoreboard.css      # ตารางคะแนนสด, ประวัติ, แท่น Podium, การ์ดเหรียญรางวัล
└── js/
    ├── app.js              # Controller หลัก ควบคุมการสลับโหมด คะแนน สถิติ Modal
    ├── wheel.js            # Engine วงล้อ Canvas 2D ฟิสิกส์การหมุน และการอ่านองศา
    ├── queue.js            # Engine โหมดเข้าแถว และ Generator สร้าง SVG ดินปั้น 6 ท่า
    ├── deduct.js           # Engine โหมดกราฟแท่งตัดคะแนน หลอด HP และ Event Handler
    ├── audio.js            # Web Audio API Synthesizer สร้างเสียงเอฟเฟกต์ทั้งหมด
    ├── confetti.js         # Canvas Particle Confetti เอฟเฟกต์พลุกระดาษ
    ├── storage.js          # จัดการบันทึกข้อมูล LocalStorage และค่าเริ่มต้น Settings
    └── export.js           # จัดการส่งออก CSV (UTF-8 BOM) และสั่งพิมพ์
```

---

## 🔧 5. สรุปจุดเชื่อมโยงทางเทคนิคที่สำคัญ (Technical Notes for Next Dev / AI)

1. **การผูก Event ของโหมดตัดคะแนน (`js/deduct.js`):**
   - ใช้ **Event Delegation** บน Container (`#deductCard`) โดยดักฟัง `click` ด้วย `e.target.closest('.btn-deduct')`
   - มีการอัปเดต DOM แบบเจาะจงรายบุคคล (Targeted DOM Update) ที่ `#deductScoreText_${student.id}` และ `#deductBarFill_${student.id}` เพื่อความลื่นไหล
2. **การทำงานร่วมกับ LocalStorage (`js/storage.js` & `js/app.js`):**
   - ข้อมูลนักเรียนแต่ละคนมีฟิลด์:
     - `score`: คะแนนสะสมจากการตอบคำถาม (โหมด 1 และ 2)
     - `behaviorScore`: คะแนนความประพฤติคงเหลือ (โหมด 3, เริ่มต้น 100)
     - `deductionsCount`: จำนวนครั้งที่ถูกตัดคะแนน (โหมด 3)
     - `answeredCount`, `correctCount`, `wrongCount`, `pickedCount`, `enabled`
3. **ขั้นตอนการนำขึ้น GitHub Pages:**
   - เมื่อทำการแก้ไขโค้ดในเครื่อง จะต้องเปิด **GitHub Desktop** แล้วกดปุ่ม **"Push origin"** ขึ้นไปยัง GitHub เสมอ จึงจะทำให้การแก้ไขมีผลบนเว็บ `https://tinala-ct.github.io/collector-point/`
   - ใน `index.html` มีการใส่ query string version เช่น `?v=1.5` เพื่อป้องกัน Browser Caching

---

เอกสารนี้สร้างขึ้นเมื่อ: 2026-09-13
สถานะโปรเจกต์: **โครงสร้างและฟังก์ชันทั้ง 3 โหมดถูกพัฒนาไว้ครบถ้วนสมบูรณ์พร้อมต่อยอด**
